name: Fix issue 105

on:
  push:
    branches:
      - fix/issue-105-chat-scene-flush

permissions:
  contents: write

jobs:
  validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/issue-105-chat-scene-flush
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Apply fix
        run: python .github/scripts/issue105.py
      - run: npm install --no-package-lock
      - run: node --test tests/navigation-save-guard.test.mjs tests/async-state-guard.test.mjs tests/chat-selection-guard.test.mjs
      - run: node --test tests/tts-service-*.test.mjs
      - run: npm run build
      - name: Commit validated fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm .github/scripts/issue105.py .github/workflows/fix-issue-105.yml
          git add -A
          git commit -m "Wait for Scene save before opening Chat"
          git push origin HEAD:fix/issue-105-chat-scene-flush
